import com.bmuschko.gradle.docker.tasks.image.DockerBuildImage

plugins {
    id 'com.bmuschko.docker-remote-api' version '4.2.0'
}

repositories {
    jcenter()
}

task buildImage(type: DockerBuildImage) {
    inputDir = project.file('.')
    tags.add('filchakova/image:latest')
}

